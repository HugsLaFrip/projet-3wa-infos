{% extends 'admin/_nav_admin.html.twig' %}

{% block body_admin %}
	<h2>Modifier catégorie "{{ category.name }}"</h2>

	<div class="buttons">
		{% include "_return.html.twig" with {'route': 'category_index'} %}
		{{ include('admin/category/_delete_form.html.twig') }}
	</div>

	{{ include('admin/category/_form.html.twig', {'button_label': 'Modifier'}) }}
{% endblock %}
