{% extends 'base.html.twig' %}

{% block body %}
	<div class="container">
		<h2>Edition article "{{ article.title }}"</h2>

		<div class="buttons">
			{% include "article/_return.html.twig" %}
			{{ include('article/_delete_form.html.twig') }}
		</div>

		{{ include('article/_form.html.twig', {'button_label': 'Éditer'}) }}
	</div>
{% endblock %}
