{% extends 'base.html.twig' %}

{% block body %}
	<h2>Créer son compte</h2>

	{{ form_start(form, {'attr': {'autocomplete': 'off'}}) }}
	{{ form_widget(form) }}
	<button type="submit" class="btn_submit">Créer son compte</button>
	{{ form_end(form) }}
{% endblock %}
