{{ form_start(form, {'attr': {'autocomplete': 'off', 'class': 'form'}}) }}
{{ form_widget(form) }}
<button type="submit" class="btn btn_submit">{{ button_label|default('Envoyer') }}</button>
{{ form_end(form) }}
